import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'rs-cleanup-solo',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
